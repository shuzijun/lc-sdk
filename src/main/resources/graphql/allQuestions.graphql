query allQuestions {
    allQuestions {
        title
        titleSlug
        frontendQuestionId: questionFrontendId
        questionId
        status
        difficulty
        isPaidOnly
        category: categoryTitle
    }
}